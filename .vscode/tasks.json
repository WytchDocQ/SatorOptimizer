{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SATOR: Start Server",
            "type": "shell",
            "command": "${workspaceFolder}\\venv\\Scripts\\python.exe -m sator_os_engine.server.main",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "SATOR: Run Tests",
            "type": "shell",
            "command": "${workspaceFolder}\\venv\\Scripts\\python.exe -m pytest -q",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}

